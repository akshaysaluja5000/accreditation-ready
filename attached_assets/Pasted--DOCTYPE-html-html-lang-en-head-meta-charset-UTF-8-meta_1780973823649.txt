<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AccreditationReady — Leadership View</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600&family=DM+Sans:opsz,wght@9..40,300;9..40,400;9..40,500;9..40,600&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --navy: #1B2A4A;
    --navy-mid: #243455;
    --gold: #C9A84C;
    --green: #2D7A4F;
    --green-bg: #EBF5EF;
    --amber: #B86E00;
    --amber-bg: #FEF3E2;
    --red: #B93333;
    --red-bg: #FDEEEC;
    --blue: #1B5FAA;
    --blue-bg: #EBF1FA;
    --slate: #6B7C96;
    --border: #E4E9F0;
    --bg: #F2F5F9;
    --white: #FFFFFF;
    --text: #1B2A4A;
  }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    font-size: 14px;
  }

  /* HEADER */
  header {
    background: var(--navy);
    padding: 0 28px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 60px;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 2px 12px rgba(0,0,0,0.15);
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .logo-mark {
    width: 34px; height: 34px;
    border: 1.5px solid var(--gold);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-family: 'Playfair Display', serif;
    font-size: 12px; font-weight: 600;
    color: var(--gold);
  }

  .logo-text {
    font-size: 14px; font-weight: 400; color: #fff;
  }
  .logo-text strong { font-weight: 600; }

  .header-center {
    position: absolute;
    left: 50%; transform: translateX(-50%);
    font-size: 12px;
    color: rgba(255,255,255,0.5);
    letter-spacing: 0.3px;
  }

  .header-right {
    display: flex; align-items: center; gap: 14px;
  }

  .facility-pill {
    background: rgba(255,255,255,0.1);
    border: 1px solid rgba(255,255,255,0.18);
    border-radius: 20px;
    padding: 4px 12px;
    font-size: 11px;
    color: rgba(255,255,255,0.75);
    font-weight: 500;
  }

  .avatar {
    width: 32px; height: 32px;
    background: var(--gold);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 12px; font-weight: 600;
    color: var(--navy);
  }

  /* LAYOUT */
  main {
    max-width: 1040px;
    margin: 0 auto;
    padding: 32px 24px 60px;
    display: flex;
    flex-direction: column;
    gap: 24px;
  }

  /* PAGE HEADER */
  .page-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    animation: fadeUp 0.4s ease both;
  }

  .page-greeting { font-size: 11px; color: var(--slate); text-transform: uppercase; letter-spacing: 1px; margin-bottom: 4px; }
  .page-title { font-family: 'Playfair Display', serif; font-size: 26px; font-weight: 600; }
  .page-sub { font-size: 12px; color: var(--slate); margin-top: 3px; }

  .last-updated {
    font-size: 11px; color: var(--slate);
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: 20px;
    padding: 5px 12px;
  }

  /* TOP ROW — score + survey window */
  .top-row {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 16px;
    animation: fadeUp 0.4s ease 0.05s both;
  }

  .score-card {
    background: var(--navy);
    border-radius: 16px;
    padding: 28px 28px 24px;
    position: relative;
    overflow: hidden;
    grid-column: span 1;
  }

  .score-card::after {
    content: '';
    position: absolute;
    bottom: -40px; right: -40px;
    width: 140px; height: 140px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(201,168,76,0.12) 0%, transparent 70%);
    pointer-events: none;
  }

  .card-eyebrow {
    font-size: 10px; font-weight: 600;
    letter-spacing: 1.2px; text-transform: uppercase;
    color: var(--gold); margin-bottom: 10px;
  }

  .big-number {
    font-family: 'Playfair Display', serif;
    font-size: 64px; font-weight: 600;
    color: #fff; line-height: 1;
    margin-bottom: 4px;
  }

  .big-number sup {
    font-size: 28px;
    font-family: 'DM Sans', sans-serif;
    font-weight: 300;
    color: rgba(255,255,255,0.45);
    vertical-align: super;
  }

  .score-status {
    display: inline-flex; align-items: center; gap: 6px;
    background: rgba(45,122,79,0.22);
    border: 1px solid rgba(111,207,151,0.3);
    border-radius: 20px;
    padding: 3px 10px;
    font-size: 11px; font-weight: 500;
    color: #6FCF97; margin-bottom: 14px;
  }

  .pulse { width: 5px; height: 5px; border-radius: 50%; background: #6FCF97; animation: pulse 2s ease infinite; }
  @keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:0.4;transform:scale(0.7)} }

  .score-desc {
    font-size: 12px; color: rgba(255,255,255,0.45); line-height: 1.5;
  }

  /* sub-score bars */
  .sub-scores { display: flex; flex-direction: column; gap: 9px; margin-top: 18px; }

  .sub-row { display: flex; flex-direction: column; gap: 3px; }

  .sub-label {
    display: flex; justify-content: space-between;
    font-size: 10px; color: rgba(255,255,255,0.4);
    text-transform: uppercase; letter-spacing: 0.7px;
  }
  .sub-label span { color: rgba(255,255,255,0.7); font-weight: 500; }

  .sub-bar { height: 3px; background: rgba(255,255,255,0.1); border-radius: 2px; }
  .sub-fill { height: 100%; border-radius: 2px; }

  /* SURVEY WINDOW CARD */
  .survey-card {
    background: var(--white);
    border-radius: 16px;
    padding: 24px;
    border: 1px solid var(--border);
    display: flex; flex-direction: column; justify-content: space-between;
  }

  .countdown-big {
    font-family: 'Playfair Display', serif;
    font-size: 52px; font-weight: 600;
    color: var(--navy); line-height: 1;
    margin: 10px 0 4px;
  }

  .countdown-unit {
    font-size: 14px; color: var(--slate); font-weight: 300;
  }

  .survey-detail {
    margin-top: 16px;
    padding-top: 16px;
    border-top: 1px solid var(--border);
    font-size: 11px; color: var(--slate); line-height: 1.6;
  }

  .survey-detail strong { color: var(--navy); font-weight: 500; }

  /* QUICK WINS CARD */
  .quick-card {
    background: var(--white);
    border-radius: 16px;
    padding: 24px;
    border: 1px solid var(--border);
  }

  .quick-item {
    display: flex; align-items: flex-start; gap: 10px;
    padding: 8px 0;
    border-bottom: 1px solid var(--border);
    cursor: pointer;
  }
  .quick-item:last-child { border-bottom: none; padding-bottom: 0; }

  .quick-check {
    width: 18px; height: 18px; min-width: 18px;
    border: 1.5px solid var(--border);
    border-radius: 4px; margin-top: 1px;
  }

  .quick-text { font-size: 12px; color: var(--text); line-height: 1.5; }
  .quick-text em { font-style: normal; color: var(--slate); font-size: 11px; display: block; margin-top: 1px; }

  /* NEEDS ATTENTION */
  .attention-row {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 16px;
    animation: fadeUp 0.4s ease 0.1s both;
  }

  .alert-card {
    background: var(--white);
    border-radius: 16px;
    padding: 20px 20px 18px;
    border: 1px solid var(--border);
    border-top: 3px solid transparent;
  }

  .alert-card.red { border-top-color: var(--red); }
  .alert-card.amber { border-top-color: var(--amber); }
  .alert-card.blue { border-top-color: var(--blue); }

  .alert-icon-row {
    display: flex; align-items: center; gap: 8px;
    margin-bottom: 12px;
  }

  .alert-icon {
    width: 32px; height: 32px; border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    font-size: 14px;
  }

  .alert-card.red .alert-icon { background: var(--red-bg); }
  .alert-card.amber .alert-icon { background: var(--amber-bg); }
  .alert-card.blue .alert-icon { background: var(--blue-bg); }

  .alert-tag {
    font-size: 10px; font-weight: 600; letter-spacing: 0.8px;
    text-transform: uppercase; padding: 2px 8px; border-radius: 10px;
  }

  .alert-card.red .alert-tag { background: var(--red-bg); color: var(--red); }
  .alert-card.amber .alert-tag { background: var(--amber-bg); color: var(--amber); }
  .alert-card.blue .alert-tag { background: var(--blue-bg); color: var(--blue); }

  .alert-number {
    font-family: 'Playfair Display', serif;
    font-size: 36px; font-weight: 600; line-height: 1;
    margin-bottom: 2px;
  }

  .alert-card.red .alert-number { color: var(--red); }
  .alert-card.amber .alert-number { color: var(--amber); }
  .alert-card.blue .alert-number { color: var(--blue); }

  .alert-title { font-size: 13px; font-weight: 500; color: var(--navy); margin-bottom: 6px; }
  .alert-detail { font-size: 11px; color: var(--slate); line-height: 1.5; }

  .alert-items { margin-top: 10px; display: flex; flex-direction: column; gap: 5px; }

  .alert-item {
    font-size: 11px; color: var(--slate);
    background: var(--bg);
    border-radius: 6px; padding: 5px 9px;
    border-left: 2px solid transparent;
    line-height: 1.4;
  }

  .alert-card.red .alert-item { border-left-color: var(--red); }
  .alert-card.amber .alert-item { border-left-color: var(--amber); }
  .alert-card.blue .alert-item { border-left-color: var(--blue); }

  .alert-item strong { color: var(--text); font-weight: 500; }

  /* DEPT + POLICY ROW */
  .mid-row {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: 16px;
    animation: fadeUp 0.4s ease 0.15s both;
  }

  .panel {
    background: var(--white);
    border-radius: 16px;
    border: 1px solid var(--border);
    overflow: hidden;
  }

  .panel-header {
    padding: 16px 20px;
    border-bottom: 1px solid var(--border);
    display: flex; justify-content: space-between; align-items: center;
  }

  .panel-title { font-size: 13px; font-weight: 600; color: var(--navy); }
  .panel-sub { font-size: 11px; color: var(--slate); }

  .dept-row {
    padding: 12px 20px;
    display: grid;
    grid-template-columns: 1fr 100px 44px;
    align-items: center;
    gap: 14px;
    border-bottom: 1px solid var(--border);
    transition: background 0.12s;
  }
  .dept-row:last-child { border-bottom: none; }
  .dept-row:hover { background: #FAFBFC; }

  .dept-name { font-size: 13px; font-weight: 500; }
  .dept-sub { font-size: 10px; color: var(--slate); margin-top: 1px; }

  .bar-wrap { height: 5px; background: var(--border); border-radius: 3px; overflow: hidden; }
  .bar-fill { height: 100%; border-radius: 3px; }

  .dept-pct { font-size: 13px; font-weight: 600; text-align: right; }

  /* POLICY ROWS */
  .policy-row {
    padding: 11px 20px;
    border-bottom: 1px solid var(--border);
    display: flex; justify-content: space-between; align-items: center;
    gap: 10px;
    transition: background 0.12s;
  }
  .policy-row:last-child { border-bottom: none; }
  .policy-row:hover { background: #FAFBFC; }

  .policy-name { font-size: 12px; font-weight: 500; color: var(--text); }
  .policy-due { font-size: 10px; color: var(--slate); margin-top: 1px; }

  .status-pill {
    font-size: 10px; font-weight: 600;
    letter-spacing: 0.4px; text-transform: uppercase;
    padding: 3px 9px; border-radius: 20px; white-space: nowrap;
  }

  .pill-red { background: var(--red-bg); color: var(--red); }
  .pill-amber { background: var(--amber-bg); color: var(--amber); }
  .pill-green { background: var(--green-bg); color: var(--green); }

  /* RECENT ACTIVITY */
  .activity-panel {
    animation: fadeUp 0.4s ease 0.2s both;
  }

  .activity-list { display: flex; flex-direction: column; }

  .activity-item {
    padding: 13px 20px;
    display: flex; align-items: flex-start; gap: 14px;
    border-bottom: 1px solid var(--border);
  }
  .activity-item:last-child { border-bottom: none; }

  .activity-dot-wrap { display: flex; flex-direction: column; align-items: center; gap: 4px; padding-top: 4px; }
  .activity-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
  .activity-line { width: 1px; height: 100%; background: var(--border); flex-grow: 1; }

  .activity-content { flex: 1; }
  .activity-text { font-size: 12px; color: var(--text); line-height: 1.5; }
  .activity-text strong { font-weight: 500; }
  .activity-time { font-size: 10px; color: var(--slate); margin-top: 2px; }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* RESPONSIVE */
  @media (max-width: 768px) {
    .top-row, .attention-row, .mid-row { grid-template-columns: 1fr; }
    .page-header { flex-direction: column; align-items: flex-start; gap: 10px; }
    .header-center { display: none; }
  }
</style>
</head>
<body>

<header>
  <div class="logo">
    <div class="logo-mark">AR</div>
    <div class="logo-text">Accreditation <strong>Ready</strong></div>
  </div>
  <div class="header-center">Riverside Surgery Center &nbsp;·&nbsp; AAAHC</div>
  <div class="header-right">
    <div class="facility-pill">Leadership View</div>
    <div class="avatar">FK</div>
  </div>
</header>

<main>

  <!-- PAGE HEADER -->
  <div class="page-header">
    <div>
      <div class="page-greeting">Saturday, June 6, 2026</div>
      <div class="page-title">Good evening, Fawn.</div>
      <div class="page-sub">Here's where your facility stands today.</div>
    </div>
    <div class="last-updated">↻ &nbsp;Updated 2 hours ago</div>
  </div>

  <!-- TOP ROW -->
  <div class="top-row">

    <!-- READINESS SCORE -->
    <div class="score-card">
      <div class="card-eyebrow">Survey Readiness Score</div>
      <div class="big-number">87<sup>%</sup></div>
      <div class="score-status"><div class="pulse"></div> On Track · AAAHC</div>
      <div class="score-desc">Your facility is in good standing. Three items need attention before your survey window opens.</div>
      <div class="sub-scores">
        <div class="sub-row">
          <div class="sub-label">Staff Training <span>91%</span></div>
          <div class="sub-bar"><div class="sub-fill" style="width:91%;background:#6FCF97;"></div></div>
        </div>
        <div class="sub-row">
          <div class="sub-label">Policies & Documents <span>84%</span></div>
          <div class="sub-bar"><div class="sub-fill" style="width:84%;background:var(--gold);"></div></div>
        </div>
        <div class="sub-row">
          <div class="sub-label">Facility Logs & Records <span>79%</span></div>
          <div class="sub-bar"><div class="sub-fill" style="width:79%;background:#F2994A;"></div></div>
        </div>
      </div>
    </div>

    <!-- SURVEY COUNTDOWN -->
    <div class="survey-card">
      <div>
        <div class="card-eyebrow" style="color:var(--navy)">Next Survey Window</div>
        <div class="countdown-big">118</div>
        <div class="countdown-unit">days until window opens</div>
      </div>
      <div class="survey-detail">
        <strong>Oct – Dec 2026</strong> · AAAHC unannounced survey<br>
        Current accreditation expires <strong>Feb 2027</strong><br>
        Accrediting body: <strong>AAAHC</strong>
      </div>
    </div>

    <!-- THIS WEEK'S PRIORITIES -->
    <div class="quick-card">
      <div class="card-eyebrow" style="color:var(--navy);margin-bottom:12px;">Your Priorities This Week</div>
      <div class="quick-item">
        <div class="quick-check"></div>
        <div class="quick-text">Review and approve the Infection Control policy
          <em>Overdue since May 15 · takes ~10 min</em>
        </div>
      </div>
      <div class="quick-item">
        <div class="quick-check"></div>
        <div class="quick-text">Remind Environmental Services team to complete training
          <em>3 of 4 staff not yet started</em>
        </div>
      </div>
      <div class="quick-item">
        <div class="quick-check"></div>
        <div class="quick-text">Confirm BLS renewal scheduled for 5 staff
          <em>Certifications lapse within 30 days</em>
        </div>
      </div>
      <div class="quick-item">
        <div class="quick-check"></div>
        <div class="quick-text">Review QAPI meeting minutes from May
          <em>Required documentation for surveyor</em>
        </div>
      </div>
    </div>

  </div>

  <!-- NEEDS ATTENTION -->
  <div style="font-size:10px;font-weight:600;letter-spacing:1.2px;text-transform:uppercase;color:var(--slate);animation:fadeUp 0.4s ease 0.08s both;">What Needs Attention</div>
  <div class="attention-row">

    <div class="alert-card red">
      <div class="alert-icon-row">
        <div class="alert-icon">📋</div>
        <div class="alert-tag">Act Now</div>
      </div>
      <div class="alert-number">3</div>
      <div class="alert-title">Policies Past Their Review Date</div>
      <div class="alert-detail">These must be reviewed and signed off annually. A surveyor will look for this.</div>
      <div class="alert-items">
        <div class="alert-item"><strong>Infection Control</strong> · overdue 22 days</div>
        <div class="alert-item"><strong>QAPI Program</strong> · overdue 8 days</div>
        <div class="alert-item"><strong>Medication Management</strong> · overdue 3 days</div>
      </div>
    </div>

    <div class="alert-card amber">
      <div class="alert-icon-row">
        <div class="alert-icon">🏥</div>
        <div class="alert-tag">This Month</div>
      </div>
      <div class="alert-number">5</div>
      <div class="alert-title">Staff BLS Certifications Expiring</div>
      <div class="alert-detail">At least one BLS-certified person must be present whenever patients are in the building.</div>
      <div class="alert-items">
        <div class="alert-item"><strong>J. Martinez, RN</strong> · expires Jun 28</div>
        <div class="alert-item"><strong>T. Okafor, RN</strong> · expires Jul 3</div>
        <div class="alert-item"><strong>+ 3 more staff</strong> · within 30 days</div>
      </div>
    </div>

    <div class="alert-card blue">
      <div class="alert-icon-row">
        <div class="alert-icon">📚</div>
        <div class="alert-tag">In Progress</div>
      </div>
      <div class="alert-number">12</div>
      <div class="alert-title">Training Modules Not Yet Completed</div>
      <div class="alert-detail">No immediate risk, but completion is required before your survey window opens.</div>
      <div class="alert-items">
        <div class="alert-item"><strong>Environmental Services</strong> · 3 staff behind</div>
        <div class="alert-item"><strong>Front Desk</strong> · 1 staff behind</div>
        <div class="alert-item"><strong>12 modules</strong> remaining total</div>
      </div>
    </div>

  </div>

  <!-- DEPT + POLICY -->
  <div class="mid-row">

    <!-- STAFF TRAINING BY DEPARTMENT -->
    <div class="panel">
      <div class="panel-header">
        <div class="panel-title">Staff Training Completion</div>
        <div class="panel-sub">by department</div>
      </div>
      <div class="dept-row">
        <div><div class="dept-name">Nursing</div><div class="dept-sub">18 staff · all modules</div></div>
        <div class="bar-wrap"><div class="bar-fill" style="width:96%;background:var(--green);"></div></div>
        <div class="dept-pct" style="color:var(--green);">96%</div>
      </div>
      <div class="dept-row">
        <div><div class="dept-name">Anesthesia</div><div class="dept-sub">6 staff · all modules</div></div>
        <div class="bar-wrap"><div class="bar-fill" style="width:92%;background:var(--green);"></div></div>
        <div class="dept-pct" style="color:var(--green);">92%</div>
      </div>
      <div class="dept-row">
        <div><div class="dept-name">Surgical Tech</div><div class="dept-sub">8 staff · all modules</div></div>
        <div class="bar-wrap"><div class="bar-fill" style="width:88%;background:var(--green);"></div></div>
        <div class="dept-pct" style="color:var(--green);">88%</div>
      </div>
      <div class="dept-row">
        <div><div class="dept-name">Front Desk & Admin</div><div class="dept-sub">9 staff · 1 behind</div></div>
        <div class="bar-wrap"><div class="bar-fill" style="width:78%;background:var(--amber);"></div></div>
        <div class="dept-pct" style="color:var(--amber);">78%</div>
      </div>
      <div class="dept-row">
        <div><div class="dept-name">Environmental Services</div><div class="dept-sub">4 staff · 3 not started</div></div>
        <div class="bar-wrap"><div class="bar-fill" style="width:25%;background:var(--red);"></div></div>
        <div class="dept-pct" style="color:var(--red);">25%</div>
      </div>
    </div>

    <!-- POLICY STATUS -->
    <div class="panel">
      <div class="panel-header">
        <div class="panel-title">Policy Review Status</div>
        <div class="panel-sub">annual requirement</div>
      </div>
      <div class="policy-row">
        <div><div class="policy-name">Infection Control</div><div class="policy-due">Due May 15 · 22 days overdue</div></div>
        <div class="status-pill pill-red">Overdue</div>
      </div>
      <div class="policy-row">
        <div><div class="policy-name">QAPI Program</div><div class="policy-due">Due May 29 · 8 days overdue</div></div>
        <div class="status-pill pill-red">Overdue</div>
      </div>
      <div class="policy-row">
        <div><div class="policy-name">Medication Management</div><div class="policy-due">Due Jun 3 · 3 days overdue</div></div>
        <div class="status-pill pill-red">Overdue</div>
      </div>
      <div class="policy-row">
        <div><div class="policy-name">Patient Rights</div><div class="policy-due">Due Aug 12</div></div>
        <div class="status-pill pill-amber">Due Soon</div>
      </div>
      <div class="policy-row">
        <div><div class="policy-name">Emergency Management</div><div class="policy-due">Reviewed Apr 2</div></div>
        <div class="status-pill pill-green">Current</div>
      </div>
      <div class="policy-row">
        <div><div class="policy-name">Credentialing & Privileging</div><div class="policy-due">Reviewed Mar 18</div></div>
        <div class="status-pill pill-green">Current</div>
      </div>
    </div>

  </div>

  <!-- RECENT ACTIVITY -->
  <div class="panel activity-panel">
    <div class="panel-header">
      <div class="panel-title">Recent Activity</div>
      <div class="panel-sub">last 7 days</div>
    </div>
    <div class="activity-list">
      <div class="activity-item">
        <div class="activity-dot-wrap"><div class="activity-dot" style="background:var(--green);"></div></div>
        <div class="activity-content">
          <div class="activity-text"><strong>Emergency Management policy</strong> reviewed and approved by S. Chen, DON</div>
          <div class="activity-time">Today · 10:14 AM</div>
        </div>
      </div>
      <div class="activity-item">
        <div class="activity-dot-wrap"><div class="activity-dot" style="background:var(--green);"></div></div>
        <div class="activity-content">
          <div class="activity-text"><strong>14 staff</strong> in Nursing completed the Infection Prevention & Control training module</div>
          <div class="activity-time">Yesterday · 3:42 PM</div>
        </div>
      </div>
      <div class="activity-item">
        <div class="activity-dot-wrap"><div class="activity-dot" style="background:var(--amber);"></div></div>
        <div class="activity-content">
          <div class="activity-text"><strong>BLS expiration alert</strong> sent to 5 staff members — no action taken yet</div>
          <div class="activity-time">Jun 4 · 8:00 AM</div>
        </div>
      </div>
      <div class="activity-item">
        <div class="activity-dot-wrap"><div class="activity-dot" style="background:var(--green);"></div></div>
        <div class="activity-content">
          <div class="activity-text"><strong>New hire onboarding</strong> completed for 2 surgical techs — all required modules assigned</div>
          <div class="activity-time">Jun 3 · 11:20 AM</div>
        </div>
      </div>
      <div class="activity-item">
        <div class="activity-dot-wrap"><div class="activity-dot" style="background:var(--red);"></div></div>
        <div class="activity-content">
          <div class="activity-text"><strong>Medication Management policy</strong> flagged as overdue — not yet reviewed</div>
          <div class="activity-time">Jun 3 · 8:00 AM</div>
        </div>
      </div>
    </div>
  </div>

</main>

</body>
</html>